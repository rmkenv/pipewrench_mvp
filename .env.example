# PipeWrench AI Environment Variables

# Required: Anthropic API Key
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Claude Model Version (defaults to claude-3-5-sonnet-20241022)
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# Optional: Maximum file upload size in MB (defaults to 10)
MAX_FILE_SIZE_MB=10

# Optional: Maximum text content size in characters (defaults to 100000)
MAX_TEXT_CHARS=100000

# Optional: Session timeout in hours (defaults to 24)
SESSION_TIMEOUT_HOURS=24

# Optional: Enable debug logging (defaults to false)
DEBUG=false

# Optional: Application environment (development, staging, production)
ENVIRONMENT=production

# Optional: Rate limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=30
