# Core Web Framework
fastapi==0.115.4
uvicorn[standard]==0.32.0

# Security-Critical Updates (Recommended)
python-multipart>=0.0.18  # CRITICAL Security Fix (was 0.0.12)
jinja2>=3.1.6            # Security Fix (was 3.1.4)

# HTTP Clients
httpx==0.27.2
requests==2.32.3
certifi==2024.8.30
urllib3==2.2.3

# LLM/AI SDKs
google-genai>=0.16.0    # Required for the Gemini code provided previously
openai==1.54.3          # Keeping your specified version, but check for latest

# Data/Utility
pydantic==2.10.1
pypdf==5.1.0
